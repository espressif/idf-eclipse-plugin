# Default build wait before the timeout exception is thrown by the test
default.project.build.wait=800000

# Default project copy delay before the timeout exception is thrown
default.project.copy.wait=7000
